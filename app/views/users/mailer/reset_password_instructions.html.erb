<p>こんにちは <%= @resource.email %>!</p>

<p>パスワード変更を要求されましたので、以下のリンクから行なってください。</p>

<p><%= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>パスワード変更を要求していない場合はこのメールを無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまでパスワードは変更されません。</p>
